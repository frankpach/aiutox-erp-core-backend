"""Shared icon token catalog for module navigation publication."""

from __future__ import annotations

MODULE_NAVIGATION_ICON_TOKENS: frozenset[str] = frozenset(
    {
        "calendar",
        "grid",
        "home",
        "plug",
        "settings",
    }
)
