"""{{ name|title }} schemas for API requests and responses."""

from datetime import datetime
from uuid import UUID

from pydantic import BaseModel, Field


class {{ name|title }}Base(BaseModel):
    """Base {{ name|lower }} schema with common fields."""

    name: str = Field(..., description="{{ name|title }} name")


class {{ name|title }}Create({{ name|title }}Base):
    """Schema for creating a new {{ name|lower }}."""

    pass


class {{ name|title }}Update(BaseModel):
    """Schema for updating a {{ name|lower }}."""

    name: str | None = None


class {{ name|title }}Response({{ name|title }}Base):
    """Schema for {{ name|lower }} response."""

    id: UUID
    created_at: datetime
    updated_at: datetime

    class Config:
        from_attributes = True

